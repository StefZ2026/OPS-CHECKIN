import * as React from "react"
// Simplified mock tooltip provider for completeness since it was in App.tsx
export function TooltipProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
